package homework;

public interface Conductive {
	/*
	 * Interface for all conductors. Conductor can lead a signal and change it
	 * using its own modifiers.
	 */
	public Double leadSignal(Signal signal);
}
